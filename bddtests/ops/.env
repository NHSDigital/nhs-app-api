# These variables are used for CI for string interpollation; we find/replace the latest value with the git commit of the package we would like to build
BACKEND_TAG=latest
WEB_TAG=latest
REDIS_DATA_TAG=latest
WIREMOCK_TAG=2.17.0-alpine
REDIS_TAG=4.0.8-r7
