server {
  listen      444 http2 ssl;
  server_name 0.0.0.0;

  ssl_certificate     /etc/ssl/local-development-https.crt;
  ssl_certificate_key /etc/ssl/key.pem;

  location / {
    proxy_pass          http://api.local.bitraft.io:8082/;
    proxy_redirect      off;
  }
}

