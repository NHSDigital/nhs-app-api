apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: 'true'
  name: nhsonline-web
spec:
  rules:
    - host: www-preview.nonliveeuw.bitraft.io
      http:
        paths:
          - backend:
              serviceName: nhsonline-web
              servicePort: 80
            path: /
  tls:
    - hosts:
      - "www-preview.nonliveeuw.bitraft.io"
      secretName: www-preview-nonliveeuw-bitraft-io
