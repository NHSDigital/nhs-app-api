const questionTypes = {
  ATTACHMENT: 'attachment',
  BOOLEAN: 'boolean',
  CHOICE: 'choice',
  DATE: 'date',
  DATETIME: 'dateTime',
  DECIMAL: 'decimal',
  DISPLAY: 'display',
  IMAGE: 'image',
  INTEGER: 'integer',
  MULTIPLE_CHOICE: 'multiple_choice',
  QUANTITY: 'quantity',
  STRING: 'string',
  TEXT: 'text',
  TIME: 'time',
};

export const {
  ATTACHMENT,
  BOOLEAN,
  CHOICE,
  DATE,
  DATETIME,
  DECIMAL,
  DISPLAY,
  IMAGE,
  INTEGER,
  MULTIPLE_CHOICE,
  QUANTITY,
  STRING,
  TEXT,
  TIME,
} = questionTypes;

export default questionTypes;
