import { INDEX_CRUMB } from '@/breadcrumbs/general';

const UPLIFT_SILVER_INTEGRATION_CRUMB = {
  defaultCrumb: [INDEX_CRUMB],
  nativeDisabled: true,
};

const FEATURE_NOT_AVAILABLE_CRUMB = {
  defaultCrumb: [INDEX_CRUMB],
  nativeDisabled: true,
};

export default {
  UPLIFT_SILVER_INTEGRATION_CRUMB,
  FEATURE_NOT_AVAILABLE_CRUMB,
};
