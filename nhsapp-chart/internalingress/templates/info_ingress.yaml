---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: 'false'
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/use-regex: "true"
  name: cold-info
  namespace: {{ .Values.global.environment }}
  labels:
    role: cold-info
    releaseVersion: {{ default "develop" .Values.messages.version }}
spec:
  rules:
    - host: api-cold.{{ .Values.zone }}.{{ .Values.domain }}
      http:
        paths:
          - path: /v[1,2]/api/users/info
            pathType: ImplementationSpecific
            backend:
             service:
               name: cold-info
               port:
                number: {{ .Values.defaultServicePort }}
          - path: /v[1,2]/api/users/me/info
            pathType: ImplementationSpecific
            backend:
             service:
              name: cold-info
              port:
                number: {{ .Values.defaultServicePort }}
  tls:
    - hosts:
        - api-cold.{{ .Values.zone }}.{{ .Values.domain }}
      secretName: {{ .Values.zone }}-{{ .Values.domain | replace "." "-" }}
