---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "nhsapp.name" . }}-{{ $.Values.info.role }}-{{ .Chart.AppVersion | replace "." "-" }}
  namespace: {{ $.Values.global.environment }}
  labels:
    app: {{ template "nhsapp.name" . }}-{{ $.Values.info.role }}
    version: {{ .Chart.AppVersion | quote }}
spec:
  selector:
    matchLabels:
      app: {{ template "nhsapp.name" . }}-{{ $.Values.info.role }}
      version: {{ .Chart.AppVersion | quote }}
  template:
    metadata:
      labels:
        app: {{ template "nhsapp.name" . }}-{{ $.Values.info.role }}
        role: {{ $.Values.info.role }}
        version: {{ .Chart.AppVersion | quote }}
      annotations:
        chaos.alpha.kubernetes.io/enabled: "true"
    spec:
      volumes:
      - name: session-key
        secret:
          secretName: session-key-{{ .Chart.AppVersion }}
      - name: wildcard-namespace-cert
        secret:
          secretName: wildcard-{{ $.Values.global.environment }}-tls-{{ .Chart.AppVersion }}
      - name: internal-ca-public-key
        secret:
          secretName: internal-ca-public-key
      - name: sidecar-proxy-config
        configMap:
          name: info-sidecar-proxy-config-{{ .Chart.AppVersion }}
      - name: nhslogin-key
        secret:
          secretName: {{ $.Values.nhsLoginKeyName }}-{{ .Chart.AppVersion }}
      containers:
      - name: info
        image: {{ $.Values.registry }}/{{ $.Values.info.image }}:{{ $.Values.info.image_tag }}
        imagePullPolicy: {{ $.Values.info.imagePullPolicy }}
        ports:
        - containerPort: {{ $.Values.info.containerPort }}
        resources:
          requests:
            memory: {{ $.Values.info.memoryRequest }}
            cpu: {{ $.Values.info.cpuRequest }}
          limits:
            memory: {{ $.Values.info.memoryLimit }}
            cpu: {{ $.Values.info.cpuLimit }}
        env:
{{- with $.Values.info.environmentVariables }}
{{ toYaml . | trim | indent 8 }}
{{- end }}
        - name: AUDIT_SINK_TYPE
          value: MONGO
        - name: PORT
          value: "{{ $.Values.info.containerPort }}"
        - name: INPUT_FOLDER_PATH
          value: "{{ $.Values.info.inputPath }}"
        - name: ASPNETCORE_URLS
          value: "http://*:{{ $.Values.info.containerPort }}"
        - name: INFO_BASE_URL
          value: "https://nhsapp-info-{{ .Chart.AppVersion | replace "." "-" }}.{{ $.Values.global.environment }}.svc.cluster.local:{{ $.Values.sidecar_proxy.containerPort }}"
        - name: VERSION_TAG
          value: "{{ $.Values.info.image_tag }}"
        - name: SESSION_ENCRYPTION_KEY_FILE
          value: "/secrets/session-key/session.key"
        - name: CORS_AUTHORITY
          value: "https://*.{{ $.Values.domain }}"
        - name: DEVICES_MONGO_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: cosmosdb-mongodb-connection-string-{{ .Chart.AppVersion }}
              key: key
        - name: AUDIT_MONGO_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: cosmosdb-mongodb-connection-string-{{ .Chart.AppVersion }}
              key: key
        - name: AUDIT_MONGO_DATABASE_NAME
        {{- if $.Values.cosmosDBName }}
          value: {{ $.Values.cosmosDBName }}-mongo
        {{- else }}
          value: {{ $.Values.global.environment }}-mongo
        {{- end }}
        - name: AUDIT_MONGO_DATABASE_COLLECTION
          value: {{ $.Values.info.mongoDatabaseInfoAuditCollection }}
        - name: USERINFO_MONGO_DATABASE_NAME
          value: {{ $.Values.cosmosDBName }}-mongo
        - name: USERINFO_MONGO_DATABASE_COLLECTION
          value: {{ $.Values.info.mongoDatabaseInfoCollection }}
        {{- if (and ($.Values.environmentStubbed) ($.Values.environmentStubbed.cidStubbed)) }}
        - name: CITIZEN_ID_JWT_ISSUER
          value: "https://nhsapp-wiremock-{{ .Chart.AppVersion | replace "." "-" }}.{{ $.Values.global.environment }}.svc.cluster.local:{{ $.Values.sidecar_proxy.containerPort }}/citizenid/"
        {{- else }}
        - name: CITIZEN_ID_JWT_ISSUER
          value: "{{ $.Values.cidProtocol }}://{{ $.Values.cidHostname }}"
        {{- end }}
        - name: CITIZEN_ID_AUTHENTICATION_TYPE
          value: "{{ $.Values.cidAuthenticationType }}"
        {{- if (and ($.Values.environmentStubbed) ($.Values.environmentStubbed.cidStubbed)) }}
        - name: CITIZEN_ID_BASE_URL
          value: "https://nhsapp-wiremock-{{ .Chart.AppVersion | replace "." "-" }}.{{ $.Values.global.environment }}.svc.cluster.local:{{ $.Values.sidecar_proxy.containerPort }}/citizenid/"
        {{- else }}
        - name: CITIZEN_ID_BASE_URL
          value: "https://iproxy-cid-{{ .Chart.AppVersion | replace "." "-" }}.{{ $.Values.global.environment }}.svc.cluster.local:{{ $.Values.iproxy.containerPort }}"
        {{- end }}
        - name: CITIZEN_ID_CLIENT_ID
          value: "{{ $.Values.cidClientID }}"
        {{- if (and ($.Values.environmentStubbed) ($.Values.environmentStubbed.emisStubbed)) }}
        - name: QUALTRICS_BASE_URL
          value: "https://nhsapp-wiremock-{{ .Chart.AppVersion | replace "." "-" }}.{{ $.Values.global.environment }}.svc.cluster.local:{{ $.Values.sidecar_proxy.containerPort }}/qualtrics/"
        {{- else }}
        - name: QUALTRICS_BASE_URL
          value: "{{ $.Values.qualtricsBaseUrl }}"
        {{- end }}
        - name: QUALTRICS_DIRECTORY_ID
          value: "{{ $.Values.qualtricsDirectoryId }}"
        - name: QUALTRICS_MAILING_LIST_ID
          value: "{{ $.Values.qualtricsMailingListId }}"
        - name: NHSLOGIN_KEY_PATH
          value: "/secrets/nhslogin-key/nhslogin.pem"
        - name: NHSLOGIN_KEY_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ $.Values.nhsLoginKeyName }}-password-{{ .Chart.AppVersion }}
              key: password
        - name: NHSAPP_API_KEY
          valueFrom:
            secretKeyRef:
              name: nhsapp-api-key-{{ .Chart.AppVersion }}
              key: apikey
        - name: QUALTRICS_API_KEY
          valueFrom:
            secretKeyRef:
              name: qualtrics-api-key-{{ .Chart.AppVersion }}
              key: apikey
        volumeMounts:
        - name: nhslogin-key
          mountPath: "/secrets/nhslogin-key"
          readOnly: true
        - name: session-key
          mountPath: "/secrets/session-key"
          readOnly: true
        - name: internal-ca-public-key
          mountPath: /usr/local/share/ca-certificates/internalca
          readOnly: true
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 2
        readinessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 2
      - name: proxy
        image: {{ $.Values.registry }}/{{ $.Values.sidecar_proxy.image }}:{{ $.Values.sidecar_proxy.image_tag }}
        imagePullPolicy: {{ $.Values.sidecar_proxy.imagePullPolicy }}
        ports:
        - containerPort: {{ $.Values.sidecar_proxy.containerPort }}
        resources:
          requests:
            memory: {{ $.Values.sidecar_proxy.memoryRequest }}
            cpu: {{ $.Values.sidecar_proxy.cpuRequest }}
          limits:
            memory: {{ $.Values.sidecar_proxy.memoryLimit }}
            cpu: {{ $.Values.sidecar_proxy.cpuLimit }}
        volumeMounts:
        - name: wildcard-namespace-cert
          mountPath: /etc/nginx/ssl
        - name: sidecar-proxy-config
          mountPath: /etc/nginx/nginx.conf
          subPath: nginx.conf
      hostAliases:
      - ip: "{{ trimSuffix "/32" $.Values.clusterIP }}"
        hostnames:
        - "api-{{ $.Values.global.environment }}.{{ $.Values.zone }}.{{ $.Values.domain }}"
        - "www-{{ $.Values.global.environment }}.{{ $.Values.zone }}.{{ $.Values.domain }}"
      imagePullSecrets:
      - name: azure-registry-{{ .Chart.AppVersion }}
      dnsConfig:
        options:
          - name: single-request-reopen
