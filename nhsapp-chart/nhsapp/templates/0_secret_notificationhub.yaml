{{- if $.Values.usersEnabled }}
apiVersion: v1
data:
  password: {{ .Values.notificationHubSharedAccessKey }}
kind: Secret
metadata:
  name: notification-hub-shared-access-key-{{ .Chart.AppVersion }}
  labels:
    version: {{ .Chart.AppVersion | quote }}
type: Opaque
{{- end }}