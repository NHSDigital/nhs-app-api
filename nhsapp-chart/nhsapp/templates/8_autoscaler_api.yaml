---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: {{ template "nhsapp.name" . }}-{{ $.Values.api.role }}
    role: {{ $.Values.api.role }}
    version: {{ .Chart.AppVersion | quote }}
  name: {{ template "nhsapp.name" . }}-{{ $.Values.api.role }}-{{ .Chart.AppVersion | replace "." "-" }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "nhsapp.name" . }}-{{ $.Values.api.role }}-{{ .Chart.AppVersion | replace "." "-" }}
  minReplicas: {{ $.Values.api.minReplicas }}
  maxReplicas: {{ $.Values.api.maxReplicas }}
  behavior: 
    {{ $.Values.scaling.dotnet | toYaml | indent 4 | trim }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ $.Values.api.targetCPUUtilizationPercentage }}