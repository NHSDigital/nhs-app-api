---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: {{ template "nhsapp.name" . }}-{{ $.Values.users.role }}
    role: {{ $.Values.users.role }}
    version: {{ .Chart.AppVersion | quote }}
  name: {{ template "nhsapp.name" . }}-{{ $.Values.users.role }}-{{ .Chart.AppVersion | replace "." "-" }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1beta1
    kind: Deployment
    name: {{ template "nhsapp.name" . }}-{{ $.Values.users.role }}-{{ .Chart.AppVersion | replace "." "-" }}
  minReplicas: {{ $.Values.users.minReplicas }}
  maxReplicas: {{ $.Values.users.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ $.Values.users.targetCPUUtilizationPercentage }}
