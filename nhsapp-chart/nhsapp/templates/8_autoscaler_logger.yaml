---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: {{ template "nhsapp.name" . }}-{{ $.Values.logger.role }}
    role: {{ $.Values.logger.role }}
    version: {{ .Chart.AppVersion | quote }}
  name: {{ template "nhsapp.name" . }}-{{ $.Values.logger.role }}-{{ .Chart.AppVersion | replace "." "-" }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "nhsapp.name" . }}-{{ $.Values.logger.role }}-{{ .Chart.AppVersion | replace "." "-" }}
  minReplicas: {{ $.Values.logger.minReplicas }}
  maxReplicas: {{ $.Values.logger.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ $.Values.logger.targetCPUUtilizationPercentage }}
