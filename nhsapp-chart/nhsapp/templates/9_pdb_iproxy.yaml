---
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ template "nhsapp.name" . }}-{{ $.Values.iproxy.role }}-pdb-{{ .Chart.AppVersion }}
  labels:
    version: {{ .Chart.AppVersion | quote }}
spec:
  minAvailable: {{ $.Values.iproxy.minAvailable }}
  selector:
    matchLabels:
      app: {{ template "nhsapp.name" . }}-{{ $.Values.iproxy.role }}
      version: {{ .Chart.AppVersion | quote }}
