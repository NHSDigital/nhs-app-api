server {
  listen      443 http2 ssl;
  server_name 0.0.0.0;

  ssl_certificate     /etc/ssl/local-development-https.crt;
  ssl_certificate_key /etc/ssl/key.pem;

  location / {
    proxy_pass          http://nhsonline.web:3000/;
    proxy_redirect      off;
  }
}

