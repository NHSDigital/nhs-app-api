apiVersion: v1
kind: Service
metadata:
  labels:
    app: nhsonline-backendworker
  name: nhsonline-backendworker
spec:
  selector:
    app: nhsonline-backendworker
  ports:
  - port: 80
    targetPort: 5123
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: nhsonline-wiremock
  name: nhsonline-wiremock
spec:
  selector:
    app: nhsonline-wiremock
  ports:
  - port: 8080
    targetPort: 8080
