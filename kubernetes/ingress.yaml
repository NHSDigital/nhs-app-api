apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: 'true'
  name: nhsonline-web
spec:
  rules:
    - host: www.develop.bitraft.io
      http:
        paths:
          - backend:
              serviceName: nhsonline-web
              servicePort: 80
            path: /
  tls:
    - hosts:
      - "www.develop.bitraft.io"
      secretName: www-develop-bitraft-io
