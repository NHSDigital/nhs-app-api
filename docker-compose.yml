version: '3'
volumes:
  node_modules:
services:
  nhsonline.web:
    image: nhsonline.web
    build:
      context: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
