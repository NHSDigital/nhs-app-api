version: "3.4"

services:

  web.local.bitraft.io:
    env_file:
      - ./docker/nhslogin_connectivity_sandpit.env

  pfs.local.bitraft.io:
    env_file:
      - ./docker/nhslogin_connectivity_sandpit.env