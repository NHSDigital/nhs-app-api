<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:panels="clr-namespace:NHSOnline.App.Controls.Panels;assembly=NHSOnline.App.Controls"
             x:Class="NHSOnline.App.Controls.Panels.NhsErrorCallout"
             x:Name="Root" >
    
    <ContentView.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/NhsUkColours.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <Style TargetType="panels:NhsPanelWithLabel">
                <Setter Property="Margin" Value="16,0,16,0" />
            </Style>
        </ResourceDictionary>
    </ContentView.Resources>
    
    <ContentView.Content>
        <panels:NhsPanelWithLabel LabelText="{Binding LabelText, Source={x:Reference Name=Root}}"
                                  PanelContent="{Binding PanelContent, Source={x:Reference Name=Root}}"
                                  LabelBackgroundColour="{StaticResource NhsUkRed}"
                                  LabelTextColour="{StaticResource NhsUkWhite}"
                                  PanelBackgroundColour="{StaticResource NhsUkWhite}"
                                  PanelHighlightColour="{StaticResource NhsUkRed}" />
    </ContentView.Content>
</ContentView>