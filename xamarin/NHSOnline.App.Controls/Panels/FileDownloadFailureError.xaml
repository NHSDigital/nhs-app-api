<?xml version="1.0" encoding="utf-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:panels="clr-namespace:NHSOnline.App.Controls.Panels;assembly=NHSOnline.App.Controls"
             xmlns:elements="clr-namespace:NHSOnline.App.Controls.Elements;assembly=NHSOnline.App.Controls"
             x:Class="NHSOnline.App.Controls.Panels.FileDownloadFailureError"
             x:Name="Root">
    <elements:Heading1 Text="Cannot download file" x:Name="Heading" />
    <panels:NhsErrorCallout LabelText="Error">
        <StackLayout>
            <elements:Paragraph Text="Try again. If you still cannot download this file and it’s a document from your GP health record, contact your GP surgery." />
            <elements:Paragraph Text="For all other types of documents, you’ll need to find another way to view them." />
            
            <elements:LinkLabel Text="Get help with downloading documents from your health record"
                                Margin="0,20,0,0"
                                Command="{Binding GetHelpWithDocumentDownloadingCommand, Source={x:Reference Name=Root}}" />

            <elements:LinkLabel Text="Try again"
                                Margin="0,20,0,0"
                                Command="{Binding TryAgainCommand, Source={x:Reference Name=Root}}" />
        </StackLayout>
    </panels:NhsErrorCallout>
</StackLayout>