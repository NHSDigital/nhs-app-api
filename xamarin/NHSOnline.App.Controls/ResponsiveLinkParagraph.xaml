<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:NHSOnline.App.Controls;assembly=NHSOnline.App.Controls"
             x:Class="NHSOnline.App.Controls.ResponsiveLinkParagraph"
             x:Name="Root">
    
    <VisualStateManager.VisualStateGroups>
        <VisualStateGroup x:Name="ResponsiveStates">
            <VisualState x:Name="Narrow"/>
            <VisualState x:Name="Wide">
                <VisualState.Setters>
                    <Setter TargetName="Label" Property="Style" Value="{ StaticResource LabelWide}" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>

    <View.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/NhsUkColours.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="Label" CanCascade="True">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontFamily" Value="Frutiger" />
                <Setter Property="LineHeight" Value="1.2" />
                <Setter Property="Margin" Value="0, 6" />
                <Setter Property="TextColor" Value="{StaticResource NhsUkPrimaryText}" />
            </Style>
            
            <Style x:Key="LabelWide" TargetType="Label" CanCascade="True">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="Margin" Value="0, 12" />
            </Style>
        </ResourceDictionary>
    </View.Resources>

    <ContentView.Content>
        <StackLayout>
          <Label>
            <Label.FormattedText>
                  <FormattedString>
                      <Span Text="{Binding LeadInText, Source={x:Reference Name=Root}}" />
                      <controls:LinkSpan Text="{Binding LinkText, Source={x:Reference Name=Root}}" />
                      <Span Text="{Binding LeadOutText, Source={x:Reference Name=Root}}" />
                  </FormattedString>
              </Label.FormattedText>
              <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding Tapped, Source={x:Reference Name=Root}}" />
              </Label.GestureRecognizers>
          </Label>
        </StackLayout>
    </ContentView.Content>
</ContentView>
