<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
                            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                            xmlns:controls="clr-namespace:NHSOnline.App.Controls;assembly=NHSOnline.App.Controls"
                            x:Class="NHSOnline.App.Controls.NhsWarningCallout"
                            x:Name="Root" >
    <ContentView.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="NhsUkColours.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentView.Resources>
    
    <ContentView.Content>
        <controls:NhsPanelWithLabel LabelText="{Binding LabelText, Source={x:Reference Name=Root}}"
                                    PanelContent="{Binding PanelContent, Source={x:Reference Name=Root}}"
                                    LabelBackgroundColour="{StaticResource NhsUkYellow}"
                                    LabelTextColour="{StaticResource NhsUkPrimaryText}"
                                    PanelBackgroundColour="{StaticResource NhsUkPaleYellow}" />
    </ContentView.Content>
</ContentView>