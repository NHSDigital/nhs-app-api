<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:effects="clr-namespace:NHSOnline.App.Controls.Effects;assembly=NHSOnline.App.Controls"
             xmlns:navigation="clr-namespace:NHSOnline.App.Controls.Navigation;assembly=NHSOnline.App.Controls"
             x:Class="NHSOnline.App.Navigation.Pages.NhsAppCloseAndLogoHeaderPage"
             x:Name="Root"
             BackgroundColor="{StaticResource NhsUkBlue}">
    <ContentPage.Content>
        <ContentView>
            <StackLayout BackgroundColor="{StaticResource NhsUkBlue}" Spacing="0">
                <navigation:SlimCloseAndLogoNavigationHeader 
                    CloseCommand="{Binding CloseClicked, Source={x:Reference Name=Root}}" />
                <ContentView BackgroundColor="{StaticResource NhsUkPageBackgroundGrey}"
                             Content="{Binding PageContent, Source={x:Reference Name=Root}}" 
                             VerticalOptions="FillAndExpand" >

                    <ContentView.Effects>
                        <effects:SafeAreaPaddingBottom />
                    </ContentView.Effects>
                </ContentView>
            </StackLayout>
        </ContentView>
    </ContentPage.Content>
</ContentPage>
