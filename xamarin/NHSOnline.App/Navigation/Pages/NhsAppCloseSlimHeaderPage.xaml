<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:navigation="clr-namespace:NHSOnline.App.Controls.Navigation;assembly=NHSOnline.App.Controls"
             x:Class="NHSOnline.App.Navigation.Pages.NhsAppCloseSlimHeaderPage"
             x:Name="Root"
             BackgroundColor="{StaticResource NhsUkBlue}">
    <ContentPage.Content>
        <ContentView>
            <StackLayout BackgroundColor="{StaticResource NhsUkBlue}" Spacing="0">
                <navigation:SlimCloseNavigationHeader
                    CloseCommand="{Binding CloseCommand, Source={x:Reference Name=Root}}" />

                <ContentView Content="{Binding PageContent, Source={x:Reference Name=Root}}"
                             VerticalOptions="FillAndExpand" />
            </StackLayout>
        </ContentView>
    </ContentPage.Content>
</ContentPage>