import { Given } from 'cucumber';

Given('I am not logged in', () => {
  // Presently this does not need to do anything as no login functionality has yet been implemented.
  // However, in order to make the feature file clear, it has been included and can be used when
  // authentication is implemented.
});
